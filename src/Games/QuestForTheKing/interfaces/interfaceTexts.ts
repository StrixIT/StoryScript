import { IInterfaceTexts as StortScriptInterfaceTexts, Feature as StoryScriptFeature } from 'storyScript/Interfaces/storyScript';

export interface IInterfaceTexts extends StortScriptInterfaceTexts {
    bow?: string;
    primaryWeapon?: string;
    secondaryWeapon?: string;
}