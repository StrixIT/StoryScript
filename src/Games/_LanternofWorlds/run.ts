namespace _LanternofWorlds {
    // Calling this function will bootstrap the game using our game namespace and rules and text objects.
    StoryScript.Run('_LanternofWorlds', CustomTexts(), Rules());
}